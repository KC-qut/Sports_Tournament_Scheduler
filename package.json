{
  "name": "sports-tournament-scheduler",
  "version": "1.0.0",
  "description": "Complete MERN Sport Tournament Scheduler Application",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run prod --prefix backend\" \"npm start --prefix frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "chai": "^5.1.2",
    "concurrently": "^7.0.0",
    "eslint-plugin-jest": "^29.16.6",
    "mocha": "^11.0.1"
  },
  "author": "Sports Tournament Scheduler Team",
  "license": "ISC"
}
